{
  "name": "attendance-system-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for email sending",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.5.0",
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "@types/nodemailer": "^7.0.4",
    "typescript": "^5.2.0"
  },
  "engines": {
    "node": "18"
  }
}
